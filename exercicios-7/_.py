    # comeco = None
    # fim = None
    # for i, letra in range(len_custom(string) - len(substring_antiga)):
    #     if letra == substring_antiga[0]: # inicio da substring
    #         for j, substring_letra in enumerate(substring_antiga): # testa se tem a substring completa
    #             if string[i + j] != substring_letra:
    #                 break
    #             if j == len_custom(substring_antiga) - 1: # última letra da substring
                   
        # for j, lletra in enumerate(substring_antiga):
        #     if letra != lletra:
        #         continue
        #     else:
        #         comeco = i